<h1>Products</h1>

<ul>
  <% @products.each do |product| %>
    <li>
      <%= link_to product.id, product %>
      <%= link_to product.name, product %>
    </li>
  <% end %>
</ul>

<%= link_to 'New Product',new_product_path %>

<%# <%= link_to 'Edit Product',edit_product_path %> %>

<%# <%= link_to 'New Product',product_path %>/ %>
