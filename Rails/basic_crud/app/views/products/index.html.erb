<%= flash[:notice] if flash[:notice]%>
<h1>Products</h1>
<ul>
  <% @products.each do |product| %>
    <li>
     <h3><%= link_to product.name, product,{:style =>"text-decoration:none"} %><h3>
      <%= link_to 'Show Product',product_path(product.id), class:"submit-btn" %>
    </li>
  <% end %>
</ul>

<%= link_to 'New Product',new_product_path %>

